# const typeDefs = gql`
 type User {
    id: ID!
    fullName: String!
    role: String!
    email: String!
    password: String!
    access: Boolean
 }

 input UserInput {
    fullName: String!
    email: String!
    password: String!
    role: String!
    access: Boolean
  }

  type Query {
    users: [User!]!
  }
  
  type Mutation {
    addUser(input: UserInput!) : User!
  }

# `;